<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss} [%level] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Tomcat и Spring — только WARN и выше, чтобы не засорять консоль -->
    <logger name="org.apache" level="WARN"/>
    <logger name="org.springframework" level="WARN"/>

    <!-- Скрыть 404 для /, /favicon.ico и других неизвестных путей -->
    <logger name="org.springframework.web.servlet.PageNotFound" level="ERROR"/>

    <!-- Наш код — INFO -->
    <logger name="ru.bmstu" level="INFO"/>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
